{% extends 'OverwatchHeroBundle:asset:layout.html.twig' %}

{% block content %}
    <h1>Reviews list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Rate</th>
                <th>Comment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for review in reviews %}
            <tr>
                <td><a href="{{ path('admin_review_show', { 'id': review.id }) }}">{{ review.id }}</a></td>
                <td>{{ review.rate }}</td>
                <td>{{ review.comment }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_review_show', { 'id': review.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_review_edit', { 'id': review.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_review_new') }}">Create a new review</a>
        </li>
    </ul>
{% endblock %}
